<!doctype html>
<html lang="vi">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Trang cá nhân • Tên Của Bạn</title>
  <meta name="description" content="Portfolio, blog và thông tin liên hệ của Tên Của Bạn." />
  <meta name="color-scheme" content="light dark">
  <meta property="og:title" content="Trang cá nhân • Tên Của Bạn">
  <meta property="og:description" content="Portfolio, blog và thông tin liên hệ của Tên Của Bạn.">
  <meta property="og:type" content="website">
  <meta property="og:image" content="">
  <meta property="og:url" content="https://tenban.github.io/">
  <link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='0.9em' font-size='90'>🧩</text></svg>">
  <style>
    /* ====== Thiết kế tổng quát (no-framework) ====== */
    :root {
      --bg: #0b0c10;
      --panel: #111218;
      --text: #e6e8ef;
      --muted: #a9afc3;
      --brand: #5b8cff;
      --brand-2: #7ee787;
      --border: #26293a;
      --shadow: 0 12px 30px rgba(0,0,0,.25);
    }
    @media (prefers-color-scheme: light) {
      :root {
        --bg: #f7f8fb;
        --panel: #ffffff;
        --text: #1a1f2e;
        --muted: #4a5468;
        --brand: #3b6cff;
        --brand-2: #0fa484;
        --border: #e9ecf3;
        --shadow: 0 10px 24px rgba(16,24,40,.08);
      }
    }
    html, body { height: 100%; }
    body {
      margin: 0;
      font: 16px/1.6 system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, "Apple Color Emoji", "Segoe UI Emoji";
      color: var(--text);
      background: radial-gradient(1200px 600px at 70% -10%, rgba(91,140,255,.15), transparent 40%), var(--bg);
    }
    .container { max-width: 1080px; margin: 0 auto; padding: 24px; }

    /* ====== Header & Nav ====== */
    header {
      position: sticky; top: 0; z-index: 20;
      backdrop-filter: saturate(1.2) blur(8px);
      background: color-mix(in oklab, var(--bg) 80%, transparent);
      border-bottom: 1px solid var(--border);
    }
    .nav { display: flex; align-items: center; justify-content: space-between; gap: 16px; }
    .brand { display: flex; align-items: center; gap: 12px; font-weight: 700; letter-spacing: .2px; }
    .brand .logo { width: 28px; height: 28px; display: grid; place-items: center; border-radius: 8px; background: linear-gradient(135deg, var(--brand), var(--brand-2)); box-shadow: var(--shadow); }
    .nav a { color: var(--muted); text-decoration: none; padding: 10px 12px; border-radius: 10px; }
    .nav a:hover { color: var(--text); background: color-mix(in oklab, var(--panel) 86%, transparent); }

    /* ====== Hero ====== */
    .hero { display: grid; grid-template-columns: 1.2fr .8fr; gap: 28px; align-items: center; padding: 48px 0 24px; }
    .badge { display: inline-flex; align-items: center; gap: 8px; font-size: 13px; color: var(--muted); background: color-mix(in oklab, var(--panel) 86%, transparent); border: 1px solid var(--border); padding: 6px 10px; border-radius: 999px; }
    h1 { font-size: clamp(28px, 4.4vw, 56px); line-height: 1.15; margin: 12px 0 12px; }
    .lead { font-size: clamp(16px, 1.6vw, 19px); color: var(--muted); max-width: 60ch; }
    .cta { display: flex; gap: 12px; margin-top: 20px; flex-wrap: wrap; }
    .btn { display: inline-flex; align-items: center; gap: 10px; padding: 12px 16px; border-radius: 14px; border: 1px solid var(--border); text-decoration: none; font-weight: 600; }
    .btn.primary { background: linear-gradient(135deg, var(--brand), var(--brand-2)); color: white; border-color: transparent; box-shadow: var(--shadow); }
    .btn:hover { transform: translateY(-1px); }

    .portrait {
      aspect-ratio: 1/1; border-radius: 22px; overflow: hidden; border: 1px solid var(--border);
      background:
        radial-gradient(300px 140px at 30% 20%, color-mix(in oklab, var(--brand) 24%, transparent), transparent),
        radial-gradient(240px 120px at 70% 80%, color-mix(in oklab, var(--brand-2) 26%, transparent), transparent),
        var(--panel);
      display: grid; place-items: center; color: #fff7;
      font-size: 14px; letter-spacing: .4px; text-transform: uppercase;
    }

    /* ====== Sections ====== */
    section { padding: 32px 0 8px; }
    .section-head { display: flex; align-items: end; justify-content: space-between; gap: 12px; margin-bottom: 14px; }
    .section-head h2 { margin: 0; font-size: clamp(20px, 2.6vw, 28px); }
    .muted { color: var(--muted); }

    /* ====== Cards/Grid ====== */
    .grid { display: grid; gap: 16px; }
    .grid.projects { grid-template-columns: repeat(auto-fit, minmax(260px, 1fr)); }
    .card { background: var(--panel); border: 1px solid var(--border); border-radius: 16px; padding: 18px; transition: transform .2s ease, box-shadow .2s ease; }
    .card:hover { transform: translateY(-3px); box-shadow: var(--shadow); }
    .thumb { aspect-ratio: 16/9; border-radius: 12px; background: linear-gradient(135deg, color-mix(in oklab, var(--brand) 30%, transparent), color-mix(in oklab, var(--brand-2) 24%, transparent)); border: 1px solid var(--border); }
    .card h3 { margin: 12px 0 4px; font-size: 18px; }
    .tag { display: inline-block; font-size: 12px; color: var(--muted); border: 1px dashed var(--border); padding: 4px 8px; border-radius: 999px; }

    /* ====== List ====== */
    .list { display: grid; gap: 8px; }
    .list a { color: inherit; text-decoration: none; padding: 10px 12px; border-radius: 12px; display: flex; align-items: center; justify-content: space-between; border: 1px solid var(--border); background: var(--panel); }
    .list a:hover { box-shadow: var(--shadow); transform: translateY(-2px); }

    /* ====== Footer ====== */
    footer { padding: 48px 0 24px; color: var(--muted); font-size: 14px; }

    /* ====== Responsive ====== */
    @media (max-width: 840px) {
      .hero { grid-template-columns: 1fr; }
    }
  </style>
</head>
<body>
  <header>
    <div class="container nav">
      <div class="brand">
        <span class="logo">✦</span>
        <span>Tên Của Bạn</span>
      </div>
      <nav>
        <a href="#about">Giới thiệu</a>
        <a href="#projects">Dự án</a>
        <a href="#writing">Bài viết</a>
        <a href="#contact">Liên hệ</a>
      </nav>
    </div>
  </header>

  <main class="container">
    <!-- ===== Hero ===== -->
    <section class="hero" aria-labelledby="hero-heading">
      <div>
        <span class="badge">Hi 👋 Tôi là <strong>&nbsp;Tên Của Bạn</strong></span>
        <h1 id="hero-heading">Xây dựng sản phẩm số, viết blog & chia sẻ kiến thức.</h1>
        <p class="lead">Tôi là <strong>Vị trí của bạn</strong> (VD: Frontend Developer / Designer). Đam mê tạo ra trải nghiệm web nhanh, đẹp và dễ dùng. Dưới đây là một vài dự án tiêu biểu và ghi chép cá nhân.</p>
        <div class="cta">
          <a class="btn primary" href="#projects">Xem dự án</a>
          <a class="btn" href="#contact">Liên hệ</a>
          <a class="btn" href="CV.pdf" target="_blank" rel="noopener">CV (PDF)</a>
        </div>
      </div>
      <div class="portrait">
        <!-- Thay bằng ảnh chân dung 1:1 của bạn (avatar.jpg) -->
        <span>Ảnh chân dung</span>
      </div>
    </section>

    <!-- ===== About ===== -->
    <section id="about" aria-labelledby="about-heading">
      <div class="section-head">
        <h2 id="about-heading">Về tôi</h2>
      </div>
      <p class="muted">Tóm tắt 2–4 câu về kinh nghiệm, kỹ năng và lĩnh vực bạn quan tâm. Nêu ngắn gọn mục tiêu nghề nghiệp hoặc điều bạn đang tìm kiếm (job/freelance/cộng tác…).</p>
      <ul>
        <li>🌱 Kỹ năng chính: HTML, CSS, JavaScript, React, UI/UX…</li>
        <li>🛠 Công cụ: VS Code, Figma, Git, Vercel/Netlify…</li>
        <li>🎯 Sở thích: Thiết kế hệ thống, tối ưu hiệu năng, viết blog.</li>
      </ul>
    </section>

    <!-- ===== Projects ===== -->
    <section id="projects" aria-labelledby="projects-heading">
      <div class="section-head">
        <h2 id="projects-heading">Dự án tiêu biểu</h2>
        <a class="muted" href="#">Xem tất cả →</a>
      </div>
      <div class="grid projects">
        <article class="card">
          <div class="thumb" role="img" aria-label="Ảnh minh hoạ dự án 1"></div>
          <h3>Project One</h3>
          <p class="muted">Mô tả ngắn 1–2 câu: mục tiêu, vai trò của bạn, công nghệ đã dùng.</p>
          <div class="tag">React · Vite · Netlify</div>
        </article>
        <article class="card">
          <div class="thumb" role="img" aria-label="Ảnh minh hoạ dự án 2"></div>
          <h3>Project Two</h3>
          <p class="muted">Kết quả nổi bật (KPIs): tăng 40% tốc độ tải, 99.9% uptime…</p>
          <div class="tag">Next.js · Vercel</div>
        </article>
        <article class="card">
          <div class="thumb" role="img" aria-label="Ảnh minh hoạ dự án 3"></div>
          <h3>Project Three</h3>
          <p class="muted">Link demo và mã nguồn nếu công khai.</p>
          <div class="tag">TypeScript · Cloud</div>
        </article>
      </div>
    </section>

    <!-- ===== Writing / Blog ===== -->
    <section id="writing" aria-labelledby="writing-heading">
      <div class="section-head">
        <h2 id="writing-heading">Bài viết gần đây</h2>
        <a class="muted" href="#">RSS</a>
      </div>
      <div class="list">
        <a href="#">
          <span>Thiết kế hệ thống component: từ Figma tới code</span>
          <span class="muted">12/2024</span>
        </a>
        <a href="#">
          <span>Tối ưu hiệu năng web với Core Web Vitals</span>
          <span class="muted">10/2024</span>
        </a>
        <a href="#">
          <span>Viết README cuốn hút cho dự án open-source</span>
          <span class="muted">08/2024</span>
        </a>
      </div>
    </section>

    <!-- ===== Contact ===== -->
    <section id="contact" aria-labelledby="contact-heading">
      <div class="section-head">
        <h2 id="contact-heading">Liên hệ</h2>
      </div>
      <p class="muted">Ưu tiên email, mình sẽ phản hồi sớm nhất có thể.</p>
      <div class="cta">
        <a class="btn primary" href="mailto:you@example.com">📧 you@example.com</a>
        <a class="btn" href="https://github.com/yourname" target="_blank" rel="noopener">GitHub</a>
        <a class="btn" href="https://www.linkedin.com/in/yourname" target="_blank" rel="noopener">LinkedIn</a>
        <button class="btn" id="copyEmail" type="button">Copy email</button>
      </div>
    </section>

    <footer>
      <hr style="border:0;border-top:1px solid var(--border);margin:16px 0 24px;">
      <div style="display:flex;gap:12px;align-items:center;justify-content:space-between;flex-wrap:wrap;">
        <small>© <span id="year"></span> Tên Của Bạn. Mọi quyền được bảo lưu.</small>
        <small class="muted">Made with ♥ · <a href="#" style="color:inherit;">View source</a></small>
      </div>
    </footer>
  </main>

  <script>
    // Năm hiện tại
    document.getElementById('year').textContent = new Date().getFullYear();

    // Copy email nhanh
    document.getElementById('copyEmail')?.addEventListener('click', async () => {
      const email = 'you@example.com';
      try { await navigator.clipboard.writeText(email); alert('Đã copy: ' + email); } 
      catch { prompt('Copy email thủ công:', email); }
    });
  </script>
</body>
</html>
